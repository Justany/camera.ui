# ===================================
# Configuration camera.ui - Template
# Copier ce fichier en .env et personnaliser les valeurs
# ===================================

# ===================================
# Configuration Générale
# ===================================

# Timezone (pour les logs et enregistrements)
# Format: Area/City (voir: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=Africa/Kinshasa

# Nom de l'organisation (utilisé dans les certificats SSL)
ORGANIZATION_NAME=Mvutu Security

# Domaine public (si disponible, sinon laisser camera.local)
DOMAIN_NAME=camera.local

# ===================================
# Ports Applicatifs
# ===================================

# Port interne de camera.ui (ne pas changer sauf nécessité)
CAMERA_UI_PORT=8081

# Ports externes HTTP/HTTPS (changer si ports déjà utilisés)
HTTP_PORT=80
HTTPS_PORT=443

# Ports MQTT (si utilisé pour détection de mouvement)
MQTT_PORT=1883
MQTT_WS_PORT=9001

# ===================================
# Authentification & Sécurité
# ===================================

# JWT Secret - TRÈS IMPORTANT!
# Générer avec: openssl rand -hex 32
# ⚠️ CHANGEZ CETTE VALEUR EN PRODUCTION!
JWT_SECRET=changeme-generate-strong-secret-here-with-openssl-rand-hex-32

# Identifiants par défaut (seront obligatoirement changés au premier login)
DEFAULT_USERNAME=master
DEFAULT_PASSWORD=master

# ===================================
# Notifications - Telegram (optionnel)
# ===================================

# Pour obtenir un bot token:
# 1. Parler à @BotFather sur Telegram
# 2. Créer un nouveau bot avec /newbot
# 3. Copier le token fourni
TELEGRAM_BOT_TOKEN=

# Pour obtenir votre Chat ID:
# 1. Parler à @userinfobot sur Telegram
# 2. Copier l'ID fourni
TELEGRAM_CHAT_ID=

# ===================================
# AWS Rekognition (optionnel)
# Pour la reconnaissance d'images/objets
# ===================================

# Clés d'accès AWS IAM
# Créer un utilisateur IAM avec les permissions Rekognition
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# ===================================
# SMTP pour notifications email (optionnel)
# ===================================

# Serveur SMTP (ex: smtp.gmail.com)
SMTP_HOST=

# Port SMTP (587 pour TLS, 465 pour SSL, 25 pour non sécurisé)
SMTP_PORT=587

# Utiliser une connexion sécurisée (true/false)
SMTP_SECURE=false

# Authentification SMTP
SMTP_USER=
SMTP_PASS=

# Adresse email d'envoi
SMTP_FROM=camera.ui@yourdomain.com

# ===================================
# Webhook pour notifications (optionnel)
# ===================================

# URL du webhook (Slack, Discord, etc.)
WEBHOOK_URL=

# ===================================
# Limites de ressources
# Ajuster selon les capacités de votre serveur
# ===================================

# CPU (nombre de cœurs, ex: 1, 2, 4)
CPU_LIMIT=2
CPU_RESERVATION=0.5

# Mémoire (ex: 512M, 1G, 2G)
MEMORY_LIMIT=2G
MEMORY_RESERVATION=512M

# ===================================
# Chemins de stockage
# ===================================

# Répertoire des enregistrements (sur l'hôte)
# Utiliser un SSD ou disque rapide pour de meilleures performances
RECORDINGS_PATH=./recordings

# Répertoire des sauvegardes
BACKUP_PATH=./backups

# ===================================
# Fail2ban Configuration
# Protection contre les attaques par force brute
# ===================================

# Durée de bannissement en secondes (3600 = 1 heure)
F2B_BANTIME=3600

# Fenêtre de recherche en secondes (600 = 10 minutes)
F2B_FINDTIME=600

# Nombre maximum de tentatives avant bannissement
F2B_MAXRETRY=5

# ===================================
# Enregistrements
# ===================================

# Durée de rétention des enregistrements en jours
RECORDINGS_RETENTION_DAYS=30

# Qualité des enregistrements (low, medium, high)
RECORDING_QUALITY=high

# ===================================
# Streaming
# ===================================

# Résolution de stream par défaut (ex: 1920x1080, 1280x720)
STREAM_RESOLUTION=1920x1080

# FPS (images par seconde)
STREAM_FPS=25

# Bitrate en Kbps (ex: 2000 = 2Mbps)
STREAM_BITRATE=2000

# ===================================
# Base de données (optionnel - usage futur)
# ===================================

# Type de base de données (sqlite par défaut)
DB_TYPE=sqlite

# Chemin de la base de données
DB_PATH=/app/data/database/database.json

# ===================================
# Mode Debug & Logs
# ===================================

# Activer le mode debug (true/false)
# ⚠️ Désactiver en production pour les performances
DEBUG_MODE=false

# Niveau de log (error, warn, info, debug)
LOG_LEVEL=info

# Nombre maximum de lignes dans les logs (rotation)
LOG_MAX_LINES=10000

# ===================================
# HomeKit (si utilisé avec Homebridge)
# ===================================

# Nom du bridge HomeKit
HOMEKIT_BRIDGE_NAME=Camera UI

# Code PIN HomeKit (format: XXX-XX-XXX)
HOMEKIT_PIN=123-45-678

# ===================================
# Avancé
# ===================================

# Activer les métriques Prometheus (true/false)
ENABLE_METRICS=false

# Port des métriques
METRICS_PORT=9090

# Nombre de workers FFmpeg parallèles
FFMPEG_WORKERS=2

# Timeout de connexion aux caméras (en secondes)
CAMERA_TIMEOUT=30

