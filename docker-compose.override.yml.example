# ===================================
# Docker Compose Override - Exemple
# Copier en docker-compose.override.yml pour personnaliser
# ===================================

version: '3.8'

# Ce fichier permet de surcharger la configuration
# sans modifier docker-compose.yml

services:
  # ===================================
  # Personnalisation camera-ui
  # ===================================
  camera-ui:
    # Exemple: Changer les ports
    # ports:
    #   - "8082:8081"
    
    # Exemple: Ajouter des variables d'environnement
    # environment:
    #   - DEBUG_MODE=true
    #   - LOG_LEVEL=debug
    
    # Exemple: Monter des volumes supplémentaires
    # volumes:
    #   - /mnt/nas/recordings:/app/data/recordings
    
    # Exemple: Augmenter les limites de ressources
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '4'
    #       memory: 4G

  # ===================================
  # Désactiver des services
  # ===================================
  
  # Exemple: Désactiver MQTT si non utilisé
  # mqtt-broker:
  #   profiles:
  #     - disabled
  
  # Exemple: Désactiver fail2ban si non souhaité
  # fail2ban:
  #   profiles:
  #     - disabled

  # ===================================
  # Personnalisation Nginx
  # ===================================
  nginx-proxy:
    # Exemple: Changer les ports HTTPS
    # ports:
    #   - "8443:443"
    
    # Exemple: Ajouter des domaines supplémentaires
    # environment:
    #   - VIRTUAL_HOST=camera.example.com,camera2.example.com

# ===================================
# Ajouter des services supplémentaires
# ===================================

# Exemple: Ajouter Grafana pour le monitoring
# grafana:
#   image: grafana/grafana:latest
#   container_name: mvutu-grafana
#   restart: unless-stopped
#   ports:
#     - "3000:3000"
#   volumes:
#     - grafana-data:/var/lib/grafana
#   networks:
#     - frontend

# Exemple: Ajouter Prometheus pour les métriques
# prometheus:
#   image: prom/prometheus:latest
#   container_name: mvutu-prometheus
#   restart: unless-stopped
#   ports:
#     - "9090:9090"
#   volumes:
#     - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
#     - prometheus-data:/prometheus
#   networks:
#     - frontend

# volumes:
#   grafana-data:
#   prometheus-data:

